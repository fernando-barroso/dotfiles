# Additional packages for headless Mac Mini server
# Install with: brew bundle --file=~/dotfiles/packages/Brewfile.server

brew "tailscale"       # CLI daemon — runs as system LaunchDaemon, no GUI session needed
cask "docker-desktop"
